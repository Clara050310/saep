<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8">
  <title>Registro de Usuário - SAEP</title>
  <link rel="stylesheet" href="/style.css">
  <style>
    body {
      font-family: "Poppins", sans-serif;
      background: #fff;
      color: #333;
      margin: 0;
      padding: 0;
    }

    header {
      background-color: #c93434; /* vermelho */
      color: white;
      padding: 15px;
      text-align: center;
    }

    main {
      max-width: 400px;
      margin: 40px auto;
      padding: 0 20px;
    }

    .form-card {
      background: #fff;
      border: 2px solid #c93434;
      border-radius: 20px;
      padding: 30px;
      box-shadow: 0 0 10px rgba(0,0,0,0.1);
    }

    .form-card h2 {
      text-align: center;
      color: #c93434;
      margin-bottom: 20px;
    }

    .form-card input {
      width: 100%;
      padding: 10px;
      margin: 8px 0;
      border-radius: 8px;
      border: 1px solid #ccc;
      font-size: 1em;
    }

    .form-card button {
      width: 100%;
      padding: 12px;
      border: none;
      border-radius: 12px;
      background: #c93434;
      color: white;
      font-size: 1em;
      cursor: pointer;
      margin-top: 10px;
      transition: 0.3s;
    }

    .form-card button:hover {
      background: #a82d2d;
    }

    .back-button {
      display: inline-block;
      margin-bottom: 20px;
      padding: 10px 20px;
      background: #555;
      color: white;
      border-radius: 12px;
      text-decoration: none;
      transition: 0.3s;
    }

    .back-button:hover {
      background: #333;
    }

    .erro {
      color: #c93434;
      font-weight: bold;
      text-align: center;
      margin-bottom: 10px;
    }
  </style>
</head>
<body>
  <header>
    <h1>Registro de Usuário</h1>
  </header>

  <main>
    <a href="/principal" class="back-button">Voltar</a>

    <form action="/registro" method="POST" class="form-card">
      <h2>Criar Conta</h2>

      <% if (erro) { %>
        <div class="erro"><%= erro %></div>
      <% } %>

      <input type="text" name="nome" placeholder="Nome completo" required>
      <input type="email" name="email" placeholder="E-mail" required>
      <input type="password" name="senha" placeholder="Senha" required>
      <input type="password" name="confirma_senha" placeholder="Confirmar senha" required>
      <button type="submit">Registrar</button>
    </form>
  </main>
</body>
</html>
